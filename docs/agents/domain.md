# Domain docs

Read `CONTEXT.md` before product work. Read any matching record under `docs/adr/` before changing a system-wide choice.

This is a single-context repo:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use the terms in `CONTEXT.md` in specs, tickets, tests, and UI copy. If a new term conflicts with the glossary, settle that conflict before adding it. Flag any work that conflicts with an existing decision record.
